<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Url-shortner</title>
  </head>
  <body>
    <div class="container">
      <h1>URL Shotner Project</h1>
      <form action="/shorten" method="post">
        <label for="longurl">Engter Long URL:</label>
        <input type="text" id="longurl" name="longurl" />
        <button type="submit">Shorten</button>
      </form>
      <% if(url){ %>
      <p>
        Short URL:- <a href="<%= url %>" target="_blank" /> <%= url %>
      </p> <% } %>
    </div>
  </body>
</html>
